apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-golang.fullname" . }}-test-connection"
  labels:
    {{- include "simple-golang.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple-golang.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
